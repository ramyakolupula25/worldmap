server:
  port: 8080

spring:
  application:
    name: world-info-backend

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus

management:
  endpoint:
    health:
      show-details: always